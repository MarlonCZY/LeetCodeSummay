## Trie

### Overview
- A special form of Nary tree. It is used to store strings.
- Each node represents a string
- Root node associate with an empty string
- Use cases: auto complete, spell checker

### How to represent
- Use an array with the size of 26 to represent the letter from a to z
- Use a hashmap with character as key and child node as value